CNN - Convoultional Neural Network

Why CNN
        1) reduce high comutational cost
        2) overfitting
        3) overcome loss of spatial features

Images
        Types of Images
                Gray Scale
                RGB

Padding

Stride

Pooling

medical project (x-ray) (yes / no) (0 / 1) (true / false)
***************
1) create kaggle api key and download kaggle.json file
        right top corner (profile)
                settings
                  create legacy api key
                        (kaggle.json)

2) mac book: 
        open the terminal                                                   
        mkdir -p ~/.kaggle
        mv ~/Downloads/kaggle.json ~/.kaggle/

        chmod 600 ~/.kaggle/kaggle.json

        ls ~/.kaggle
            output : kaggle.json

    windows:
            1) create folder : .kaggle (manual)
                    (or)
                mkdir %USERPROFILE%\.kaggle

            2) move kaggle.json file  (manually)

            move %USERPROFILE%\Downloads\kaggle.json %USERPROFILE%\.kaggle\

            3) icacls %USERPROFILE%\.kaggle\kaggle.json /inheritance:r

            4) dir %USERPROFILE%\.kaggle

3) download below dependencies
                opencv-python  
                kaggle==1.5.16

3) create download.py
        cnn-pneumonia-project
            download.py

4) execute download.py

# py 3.11 -m venv venv
# venv\Scripts\Activate
# pip install -r requirements.txt
# py --version

















